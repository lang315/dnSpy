<!-- Intentionally empty: see Directory.Build.props in this folder. Stops MSBuild from importing the
     repo-root Directory.Build.targets (which deletes "garbage" files/dirs from every project's output)
     into this standalone helper. -->
<Project>
</Project>
